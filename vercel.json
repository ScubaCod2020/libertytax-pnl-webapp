{
  "version": 2,
  "buildCommand": "cd angular && npm ci --no-audit --no-fund && npm run build",
  "outputDirectory": "angular/dist/angular",
  "routes": [{ "src": "/(.*)", "dest": "/index.html" }]
}
