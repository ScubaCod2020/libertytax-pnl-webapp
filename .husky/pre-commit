#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run lint-staged first
npx --yes lint-staged || exit 1

# Validate progress log presence when milestones changed
node scripts/validate-progress-log.js || exit 1

#!/usr/bin/env sh
npx --no-install lint-staged
